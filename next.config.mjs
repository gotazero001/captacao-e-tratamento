/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/captacao-e-tratamento',
  assetPrefix: '/captacao-e-tratamento/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
